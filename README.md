# Terminal File Manager

A simple Android file manager application built with Jetpack Compose and Android Storage Access Framework. The repository includes a GitHub Actions workflow that builds the debug APK on push and pull request.

## Build

```bash
./gradlew clean assembleDebug
```

## GitHub Actions

The workflow builds the app and uploads the debug APK as an artifact.
